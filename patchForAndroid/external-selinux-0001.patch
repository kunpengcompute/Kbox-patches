diff --git a/libselinux/include/selinux/selinux.h b/libselinux/include/selinux/selinux.h
index 7922d96..ec131c2 100644
--- a/libselinux/include/selinux/selinux.h
+++ b/libselinux/include/selinux/selinux.h
@@ -3,7 +3,12 @@
 
 #include <sys/types.h>
 #include <stdarg.h>
-
+#ifndef kbox_hack
+#define kbox_hack() if (1) return
+#endif
+#ifndef kbox_hack_p
+#define kbox_hack_p(p) if (1) return p
+#endif
 #ifdef __cplusplus
 extern "C" {
 #endif
diff --git a/libselinux/src/android/android.c b/libselinux/src/android/android.c
index 2e70ceb..5040a56 100644
--- a/libselinux/src/android/android.c
+++ b/libselinux/src/android/android.c
@@ -67,16 +67,19 @@ struct selabel_handle* selinux_android_service_open_context_handle(const struct
 
     sehandle = selabel_open(SELABEL_CTX_ANDROID_SERVICE,
             seopts_service, nopts);
+    kbox_hack_p(sehandle);
 
     if (!sehandle) {
         selinux_log(SELINUX_ERROR, "%s: Error getting service context handle (%s)\n",
                 __FUNCTION__, strerror(errno));
         return NULL;
     }
+#if 0
     selinux_log(SELINUX_INFO, "SELinux: Loaded service_contexts from:\n");
     for (unsigned i = 0; i < nopts; i++) {
         selinux_log(SELINUX_INFO, "    %s\n", seopts_service[i].value);
     }
+#endif
     return sehandle;
 }
 
diff --git a/libselinux/src/android/android_platform.c b/libselinux/src/android/android_platform.c
index 20bb4a9..fd21d9a 100644
--- a/libselinux/src/android/android_platform.c
+++ b/libselinux/src/android/android_platform.c
@@ -85,6 +85,7 @@ static struct selabel_handle* selinux_android_file_context(const struct selinux_
 
 struct selabel_handle* selinux_android_file_context_handle(void)
 {
+    kbox_hack_p(NULL);
     struct selinux_opt seopts_file[MAX_FILE_CONTEXT_SIZE];
     int size = 0;
     unsigned int i;
@@ -957,6 +958,7 @@ oom:
 
 int selinux_android_setcon(const char *con)
 {
+    kbox_hack_p(0);
 	int ret = setcon(con);
 	if (ret)
 		return ret;
@@ -973,6 +975,7 @@ int selinux_android_setcontext(uid_t uid,
 			       const char *seinfo,
 			       const char *pkgname)
 {
+    kbox_hack_p(0);
 	char *orig_ctx_str = NULL, *ctx_str;
 	context_t ctx = NULL;
 	int rc = -1;
@@ -1363,6 +1366,7 @@ static int selinux_android_restorecon_common(const char* pathname_orig,
                                              uid_t uid,
                                              unsigned int flags)
 {
+    kbox_hack_p(0);
     bool nochange = (flags & SELINUX_ANDROID_RESTORECON_NOCHANGE) ? true : false;
     bool verbose = (flags & SELINUX_ANDROID_RESTORECON_VERBOSE) ? true : false;
     bool recurse = (flags & SELINUX_ANDROID_RESTORECON_RECURSE) ? true : false;
@@ -1605,6 +1609,7 @@ void selinux_android_set_sehandle(const struct selabel_handle *hndl)
 
 int selinux_android_load_policy()
 {
+    kbox_hack_p(0);
 	int fd = -1;
 
 	fd = open(sepolicy_file, O_RDONLY | O_NOFOLLOW | O_CLOEXEC);
@@ -1620,6 +1625,7 @@ int selinux_android_load_policy()
 
 int selinux_android_load_policy_from_fd(int fd, const char *description)
 {
+	kbox_hack_p(0);
 	int rc;
 	struct stat sb;
 	void *map = NULL;
diff --git a/libselinux/src/checkAccess.c b/libselinux/src/checkAccess.c
index 7227ffe..dc63672 100644
--- a/libselinux/src/checkAccess.c
+++ b/libselinux/src/checkAccess.c
@@ -20,6 +20,7 @@ static void avc_init_once(void)
 }
 
 int selinux_check_access(const char *scon, const char *tcon, const char *class, const char *perm, void *aux) {
+    kbox_hack_p(0);
 	int rc;
 	security_id_t scon_id;
 	security_id_t tcon_id;
diff --git a/libselinux/src/compute_create.c b/libselinux/src/compute_create.c
index 0975aea..7f3060a 100644
--- a/libselinux/src/compute_create.c
+++ b/libselinux/src/compute_create.c
@@ -54,6 +54,7 @@ int security_compute_create_name_raw(const char * scon,
 				     const char *objname,
 				     char ** newcon)
 {
+	kbox_hack_p((*newcon = (char *)calloc(11, 1), memcpy(*newcon, "unconfined", 10), 0));
 	char path[PATH_MAX];
 	char *buf;
 	size_t size;
diff --git a/libselinux/src/getenforce.c b/libselinux/src/getenforce.c
index d909dce..91007e0 100644
--- a/libselinux/src/getenforce.c
+++ b/libselinux/src/getenforce.c
@@ -11,6 +11,7 @@
 
 int security_getenforce(void)
 {
+	kbox_hack_p(0);
 	int fd, ret, enforce = 0;
 	char path[PATH_MAX];
 	char buf[20];
diff --git a/libselinux/src/getfilecon.c b/libselinux/src/getfilecon.c
index 20bee8a..9fcee66 100644
--- a/libselinux/src/getfilecon.c
+++ b/libselinux/src/getfilecon.c
@@ -53,6 +53,7 @@ hidden_def(getfilecon_raw)
 
 int getfilecon(const char *path, char ** context)
 {
+    kbox_hack_p((*context = (char *)calloc(11, 1), memcpy(*context, "unconfined", 10), 0));
 	int ret;
 	char * rcontext = NULL;
 
diff --git a/libselinux/src/getpeercon.c b/libselinux/src/getpeercon.c
index 3a77a2d..d4512a5 100644
--- a/libselinux/src/getpeercon.c
+++ b/libselinux/src/getpeercon.c
@@ -15,7 +15,10 @@ int getpeercon_raw(int fd, char ** context)
 {
 	char *buf;
 	socklen_t size;
-	ssize_t ret;
+	// ssize_t ret;
+	(void)fd;
+
+	char log[] = "unconfined";
 
 	size = INITCONTEXTLEN + 1;
 	buf = malloc(size);
@@ -23,6 +26,10 @@ int getpeercon_raw(int fd, char ** context)
 		return -1;
 	memset(buf, 0, size);
 
+	memcpy(buf, log, sizeof(log));
+	*context = buf;
+	return 0;
+#if 0
 	ret = getsockopt(fd, SOL_SOCKET, SO_PEERSEC, buf, &size);
 	if (ret < 0 && errno == ERANGE) {
 		char *newbuf;
@@ -41,12 +48,14 @@ int getpeercon_raw(int fd, char ** context)
 	else
 		*context = buf;
 	return ret;
+#endif
 }
 
 hidden_def(getpeercon_raw)
 
 int getpeercon(int fd, char ** context)
 {
+    kbox_hack_p((*context = (char *)calloc(11, 1), memcpy(*context, "unconfined", 10), 0));
 	int ret;
 	char * rcontext;
 
diff --git a/libselinux/src/init.c b/libselinux/src/init.c
index 083638c..f679560 100644
--- a/libselinux/src/init.c
+++ b/libselinux/src/init.c
@@ -142,6 +142,7 @@ hidden_def(fini_selinuxmnt)
 
 void set_selinuxmnt(const char *mnt)
 {
+    kbox_hack();
 	selinux_mnt = strdup(mnt);
 }
 
diff --git a/libselinux/src/label.c b/libselinux/src/label.c
index eac6e36..7bf347e 100644
--- a/libselinux/src/label.c
+++ b/libselinux/src/label.c
@@ -309,6 +309,7 @@ bool selabel_hash_all_partial_matches(struct selabel_handle *rec,
 int selabel_lookup_best_match(struct selabel_handle *rec, char **con,
 			      const char *key, const char **aliases, int type)
 {
+    kbox_hack_p((*con = (char *)calloc(11, 1), memcpy(*con, "unconfined", 10), 0));
 	struct selabel_lookup_rec *lr;
 
 	if (!rec->func_lookup_best_match) {
diff --git a/libselinux/src/lgetfilecon.c b/libselinux/src/lgetfilecon.c
index db67bc6..4d2e1de 100644
--- a/libselinux/src/lgetfilecon.c
+++ b/libselinux/src/lgetfilecon.c
@@ -53,6 +53,7 @@ hidden_def(lgetfilecon_raw)
 
 int lgetfilecon(const char *path, char ** context)
 {
+    kbox_hack_p((*context = (char *)calloc(11, 1), memcpy(*context, "unconfined", 10), 0));
 	int ret;
 	char * rcontext = NULL;
 
diff --git a/libselinux/src/lsetfilecon.c b/libselinux/src/lsetfilecon.c
index 1d3b28a..2e78cba 100644
--- a/libselinux/src/lsetfilecon.c
+++ b/libselinux/src/lsetfilecon.c
@@ -29,6 +29,7 @@ hidden_def(lsetfilecon_raw)
 
 int lsetfilecon(const char *path, const char *context)
 {
+    kbox_hack_p(0);
 	int ret;
 	char * rcontext;
 
diff --git a/libselinux/src/procattr.c b/libselinux/src/procattr.c
index c6799ef..df8d2b6 100644
--- a/libselinux/src/procattr.c
+++ b/libselinux/src/procattr.c
@@ -195,6 +195,7 @@ static int getprocattrcon_raw(char ** context,
 static int getprocattrcon(char ** context,
 			  pid_t pid, const char *attr)
 {
+    kbox_hack_p((*context = (char *)calloc(11, 1), memcpy(*context, "unconfined", 10), 0));
 	int ret;
 	char * rcontext;
 
@@ -211,6 +212,7 @@ static int getprocattrcon(char ** context,
 static int setprocattrcon_raw(const char * context,
 			      pid_t pid, const char *attr)
 {
+	kbox_hack_p(0);
 	int fd;
 	ssize_t ret;
 	int errno_hold;
@@ -280,6 +282,7 @@ out:
 static int setprocattrcon(const char * context,
 			  pid_t pid, const char *attr)
 {
+    kbox_hack_p(0);
 	int ret;
 	char * rcontext;
 
diff --git a/libselinux/src/sestatus.c b/libselinux/src/sestatus.c
index ede5a28..a5c8fc6 100644
--- a/libselinux/src/sestatus.c
+++ b/libselinux/src/sestatus.c
@@ -254,6 +254,7 @@ static int fallback_cb_policyload(int policyload)
  */
 int selinux_status_open(int fallback)
 {
+    kbox_hack_p(0);
 	int	fd;
 	char	path[PATH_MAX];
 	long	pagesize;
diff --git a/libselinux/src/setrans_client.c b/libselinux/src/setrans_client.c
index fa188a8..e1dfb6b 100644
--- a/libselinux/src/setrans_client.c
+++ b/libselinux/src/setrans_client.c
@@ -290,6 +290,7 @@ static void init_context_translations(void)
 int selinux_trans_to_raw_context(const char * trans,
 				 char ** rawp)
 {
+	kbox_hack_p(0);
 	if (!trans) {
 		*rawp = NULL;
 		return 0;
@@ -416,6 +417,7 @@ hidden_def(selinux_raw_context_to_color)
 int selinux_trans_to_raw_context(const char * trans,
 				 char ** rawp)
 {
+	kbox_hack_p(0);
 	if (!trans) {
 		*rawp = NULL;
 		return 0;
