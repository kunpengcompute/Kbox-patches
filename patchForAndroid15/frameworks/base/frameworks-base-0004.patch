diff --git a/core/java/android/hardware/display/DisplayManager.java b/core/java/android/hardware/display/DisplayManager.java
index b0ea92d140a5..32181902e410 100644
--- a/core/java/android/hardware/display/DisplayManager.java
+++ b/core/java/android/hardware/display/DisplayManager.java
@@ -1187,6 +1187,11 @@ public final class DisplayManager {
                 executor);
     }
 
+    /** @hide */
+    public void resizePrimaryDisplay(int width, int height) {
+        mGlobal.resizePrimaryDisplay(width, height);
+    }
+
     /**
      * Gets the stable device display size, in pixels.
      *
diff --git a/core/java/android/hardware/display/DisplayManagerGlobal.java b/core/java/android/hardware/display/DisplayManagerGlobal.java
index 7185719abdd5..6f919d4d1c1a 100644
--- a/core/java/android/hardware/display/DisplayManagerGlobal.java
+++ b/core/java/android/hardware/display/DisplayManagerGlobal.java
@@ -807,6 +807,14 @@ public final class DisplayManagerGlobal {
         }
     }
 
+    public void resizePrimaryDisplay(int width, int height) {
+        try {
+            mDm.resizePrimaryDisplay(width, height);
+        } catch (RemoteException ex) {
+            throw ex.rethrowFromSystemServer();
+        }
+    }
+
     public void releaseVirtualDisplay(IVirtualDisplayCallback token) {
         try {
             mDm.releaseVirtualDisplay(token);
diff --git a/core/java/android/hardware/display/IDisplayManager.aidl b/core/java/android/hardware/display/IDisplayManager.aidl
index aa1539f69722..bd7a15b4e6c5 100644
--- a/core/java/android/hardware/display/IDisplayManager.aidl
+++ b/core/java/android/hardware/display/IDisplayManager.aidl
@@ -108,6 +108,9 @@ interface IDisplayManager {
     void resizeVirtualDisplay(in IVirtualDisplayCallback token,
             int width, int height, int densityDpi);
 
+    // No permissions required, but must bo same Uid as a creator
+    void resizePrimaryDisplay(int width, int height);
+
     // No permissions required but must be same Uid as the creator.
     void setVirtualDisplaySurface(in IVirtualDisplayCallback token, in Surface surface);
 
diff --git a/core/java/android/view/IWindowManager.aidl b/core/java/android/view/IWindowManager.aidl
index e5be53191e9e..95c9ddfcf240 100644
--- a/core/java/android/view/IWindowManager.aidl
+++ b/core/java/android/view/IWindowManager.aidl
@@ -121,6 +121,7 @@ interface IWindowManager
 
     @UnsupportedAppUsage
     void getInitialDisplaySize(int displayId, out Point size);
+    void getOrigDisplaySize(int displayId, out Point size);
     @UnsupportedAppUsage
     void getBaseDisplaySize(int displayId, out Point size);
     @EnforcePermission("WRITE_SECURE_SETTINGS")
diff --git a/services/core/java/com/android/server/display/DisplayManagerService.java b/services/core/java/com/android/server/display/DisplayManagerService.java
index ae33b83b49dc..ac696157bba7 100644
--- a/services/core/java/com/android/server/display/DisplayManagerService.java
+++ b/services/core/java/com/android/server/display/DisplayManagerService.java
@@ -440,6 +440,9 @@ public final class DisplayManagerService extends SystemService {
     // The virtual display adapter, or null if not registered.
     private VirtualDisplayAdapter mVirtualDisplayAdapter;
 
+    // The local display adapter, or null if not registered.
+    private LocalDisplayAdapter mLocalDisplayAdapter;
+
     // The User ID of the current user
     private @UserIdInt int mCurrentUserId;
 
@@ -1954,6 +1957,17 @@ public final class DisplayManagerService extends SystemService {
         }
     }
 
+    private void resizePrimaryDisplayInternal(int width, int height) {
+        Slog.e(TAG, "resizePrimaryDisplayInternal, width: "+ width + "height: " + height);
+        synchronized (mSyncRoot) {
+            if (mLocalDisplayAdapter == null) {
+                return;
+            }
+
+            mLocalDisplayAdapter.resizePrimaryDisplayLocked(width, height);
+        }
+    }
+
     private void setVirtualDisplaySurfaceInternal(IBinder appToken, Surface surface) {
         synchronized (mSyncRoot) {
             if (mVirtualDisplayAdapter == null) {
@@ -2016,9 +2030,10 @@ public final class DisplayManagerService extends SystemService {
         // Register default display adapters.
         synchronized (mSyncRoot) {
             // main display adapter
-            registerDisplayAdapterLocked(mInjector.getLocalDisplayAdapter(mSyncRoot, mContext,
+            mLocalDisplayAdapter = mInjector.getLocalDisplayAdapter(mSyncRoot, mContext,
                     mHandler, mDisplayDeviceRepo, mFlags,
-                    mDisplayNotificationManager));
+                    mDisplayNotificationManager);
+            registerDisplayAdapterLocked(mLocalDisplayAdapter);
 
             // Standalone VR devices rely on a virtual display as their primary display for
             // 2D UI. We register virtual display adapter along side the main display adapter
@@ -4295,6 +4310,16 @@ public final class DisplayManagerService extends SystemService {
             }
         }
 
+        @Override // Binder call
+        public void resizePrimaryDisplay(int width, int height) {
+            final long token = Binder.clearCallingIdentity();
+            try {
+                resizePrimaryDisplayInternal(width, height);
+            } finally {
+                Binder.restoreCallingIdentity(token);
+            }
+        }
+
         @Override // Binder call
         public void setVirtualDisplaySurface(IVirtualDisplayCallback callback, Surface surface) {
             if (surface != null && surface.isSingleBuffered()) {
diff --git a/services/core/java/com/android/server/display/LocalDisplayAdapter.java b/services/core/java/com/android/server/display/LocalDisplayAdapter.java
index 0570b2ab510b..a7deab2ff138 100644
--- a/services/core/java/com/android/server/display/LocalDisplayAdapter.java
+++ b/services/core/java/com/android/server/display/LocalDisplayAdapter.java
@@ -133,6 +133,14 @@ final class LocalDisplayAdapter extends DisplayAdapter {
         }
     }
 
+    public void resizePrimaryDisplayLocked(int width, int height) {
+        long physicalId = mSurfaceControlProxy.getPhysicalDisplayIds()[0];
+        LocalDisplayDevice device = mDevices.get(physicalId);
+        if (device != null) {
+            device.resizeLocked(width, height);
+        }
+    }
+
     private void tryConnectDisplayLocked(long physicalDisplayId) {
         final IBinder displayToken =
                 mSurfaceControlProxy.getPhysicalDisplayToken(physicalDisplayId);
@@ -265,6 +273,11 @@ final class LocalDisplayAdapter extends DisplayAdapter {
         // values are defined in hardware/interfaces/drm/aidl/android/hardware/drm/HdcpLevel.aidl
         private int mConnectedHdcpLevel;
 
+        private static final int PENDIND_RESIZE = 0x02;
+        private int mWidth;
+        private int mHeight;
+        private int mPendingChages;
+
         private DisplayEventReceiver.FrameRateOverride[] mFrameRateOverrides =
                 new DisplayEventReceiver.FrameRateOverride[0];
 
@@ -282,6 +295,9 @@ final class LocalDisplayAdapter extends DisplayAdapter {
             mBacklightAdapter = new BacklightAdapter(displayToken, isFirstDisplay,
                     mSurfaceControlProxy);
             mActiveSfDisplayModeAtStartId = dynamicInfo.activeDisplayModeId;
+            mWidth = mSfDisplayModes[mActiveSfDisplayModeAtStartId].width;
+            mHeight = mSfDisplayModes[mActiveSfDisplayModeAtStartId].height;
+            mPendingChages = 0;
         }
 
         @Override
@@ -546,6 +562,48 @@ final class LocalDisplayAdapter extends DisplayAdapter {
             return true;
         }
 
+        public void resizeLocked(int width, int height) {
+            Slog.i(TAG, "resizeLocked [" + mWidth + "," + mHeight + ",] -> [" + width + "," + height + "]");
+            if (mWidth != width || mHeight != height) {
+                // 保存旧尺寸用于清理
+                int oldWidth = mWidth;
+                int oldHeight = mHeight;
+                mWidth = width;
+                mHeight = height;
+                mActiveSfDisplayMode.width = mWidth;
+                mActiveSfDisplayMode.height = mHeight;
+                mInfo = null;
+                DisplayModeRecord record = new DisplayModeRecord(mWidth, mHeight, mSupportedModes.get(mActiveModeId));
+                mSupportedModes.put(mActiveModeId, record);
+                if (!updateFramebufferSize(width, height)) {
+                    Slog.e(TAG, "Failed to update framebuffer, reverting resize");
+                    // 恢复原尺寸
+                    mWidth = oldWidth;
+                    mHeight = oldHeight;
+                    return;
+                }
+
+                mPendingChages |= PENDIND_RESIZE;
+                sendDisplayDeviceEventLocked(this, DISPLAY_DEVICE_EVENT_CHANGED);
+                sendTraversalRequestLocked();
+
+                Slog.i(TAG, "Display resize completed successfully");
+            }
+        }
+
+        private boolean updateFramebufferSize(int newWidth, int newHeight) {
+            try{
+                SurfaceControl.Transaction t = new SurfaceControl.Transaction();
+                final IBinder displayToken = getDisplayTokenLocked();
+                t.setDisplaySize(displayToken, newWidth, newHeight);
+                t.apply();
+                return true;
+            } catch (Exception e) {
+                Slog.e(TAG, "Error updating framebuffer size: " + e.getMessage(), e);
+                return false;
+            }
+        }
+
         private boolean updateColorModesLocked(int[] colorModes, int activeColorMode) {
             if (colorModes == null) {
                 return false;
@@ -1424,6 +1482,10 @@ final class LocalDisplayAdapter extends DisplayAdapter {
                     alternativeRefreshRates, mode.supportedHdrTypes);
         }
 
+        DisplayModeRecord(int width, int height, DisplayModeRecord record) {
+            mMode = new Display.Mode(record.mMode.getModeId(), width, height, record.mMode.getRefreshRate());
+        }
+
         /**
          * Returns whether the mode generated by the given DisplayModes matches the mode
          * contained by the record modulo mode ID.
diff --git a/services/core/java/com/android/server/wm/DisplayContent.java b/services/core/java/com/android/server/wm/DisplayContent.java
index 21212e573ffa..0932ecffb5fd 100644
--- a/services/core/java/com/android/server/wm/DisplayContent.java
+++ b/services/core/java/com/android/server/wm/DisplayContent.java
@@ -401,6 +401,10 @@ class DisplayContent extends RootDisplayArea implements WindowManagerPolicy.Disp
     // The physical density of the display
     int mInitialDisplayDensity = 0;
 
+    int mOrigDisplayWidth = 0;
+    int mOrigDisplayHeight = 0;
+    int mOrigDisplayDensity = 0;
+
     private Point mPhysicalDisplaySize;
 
     DisplayCutout mInitialDisplayCutout;
@@ -3025,6 +3029,10 @@ class DisplayContent extends RootDisplayArea implements WindowManagerPolicy.Disp
         mInitialDisplayWidth = mDisplayInfo.logicalWidth;
         mInitialDisplayHeight = mDisplayInfo.logicalHeight;
         mInitialDisplayDensity = mDisplayInfo.logicalDensityDpi;
+        
+        mOrigDisplayWidth = mDisplayInfo.logicalWidth;
+        mOrigDisplayHeight = mDisplayInfo.logicalHeight;
+        mOrigDisplayDensity = mDisplayInfo.logicalDensityDpi;
         mInitialPhysicalXDpi = mDisplayInfo.physicalXDpi;
         mInitialPhysicalYDpi = mDisplayInfo.physicalYDpi;
         mInitialDisplayCutout = mDisplayInfo.displayCutout;
diff --git a/services/core/java/com/android/server/wm/WindowManagerService.java b/services/core/java/com/android/server/wm/WindowManagerService.java
index b8f47cce6005..d27702c3c91a 100644
--- a/services/core/java/com/android/server/wm/WindowManagerService.java
+++ b/services/core/java/com/android/server/wm/WindowManagerService.java
@@ -5920,6 +5920,17 @@ public class WindowManagerService extends IWindowManager.Stub
         }
     }
 
+    @Override
+    public void getOrigDisplaySize(int displayId, Point size) {
+        synchronized(mWindowMap) {
+            final DisplayContent displayContent = mRoot.getDisplayContent(displayId);
+            if (displayContent != null && displayContent.hasAccess(Binder.getCallingUid())) {
+                size.x = displayContent.mOrigDisplayWidth;
+                size.y = displayContent.mOrigDisplayHeight;
+            }
+        }
+    }
+
     @Override
     public void getBaseDisplaySize(int displayId, Point size) {
         synchronized (mGlobalLock) {
@@ -5942,6 +5953,7 @@ public class WindowManagerService extends IWindowManager.Stub
                 final DisplayContent displayContent = mRoot.getDisplayContent(displayId);
                 if (displayContent != null) {
                     displayContent.setForcedSize(width, height);
+                    setForcedDisplaySizeLocked(displayContent, width, height, displayContent.mBaseDisplayDensity);
                 }
             }
         } finally {
@@ -6012,6 +6024,8 @@ public class WindowManagerService extends IWindowManager.Stub
                                 displayContent.mBaseDisplayDensity,
                                 displayContent.mBaseDisplayPhysicalXDpi,
                                 displayContent.mBaseDisplayPhysicalYDpi);
+                        mDisplayManager.resizePrimaryDisplay(width, height);
+                        displayContent.reconfigureDisplayLocked();
                         changed = true;
                     }
                 } catch (NumberFormatException ex) {
@@ -6037,6 +6051,13 @@ public class WindowManagerService extends IWindowManager.Stub
         return changed;
     }
 
+    private void setForcedDisplaySizeLocked(DisplayContent displayContent, int width, int height, int density) {
+        displayContent.updateBaseDisplayMetrics(width, height, density, displayContent.mBaseDisplayPhysicalXDpi,
+                                displayContent.mBaseDisplayPhysicalYDpi);
+        mDisplayManager.resizePrimaryDisplay(width, height);
+        displayContent.reconfigureDisplayLocked();
+    }
+
     @EnforcePermission(android.Manifest.permission.WRITE_SECURE_SETTINGS)
     @Override
     public void clearForcedDisplaySize(int displayId) {
@@ -6051,6 +6072,8 @@ public class WindowManagerService extends IWindowManager.Stub
                             displayContent.mInitialDisplayHeight,
                             displayContent.mInitialPhysicalXDpi,
                             displayContent.mInitialPhysicalYDpi);
+                    setForcedDisplaySizeLocked(displayContent, displayContent.mOrigDisplayWidth, 
+                            displayContent.mOrigDisplayHeight, displayContent.mOrigDisplayDensity);
                 }
             }
         } finally {
diff --git a/services/core/java/com/android/server/wm/WindowManagerShellCommand.java b/services/core/java/com/android/server/wm/WindowManagerShellCommand.java
index 06d8c370b914..1ae5fd802285 100644
--- a/services/core/java/com/android/server/wm/WindowManagerShellCommand.java
+++ b/services/core/java/com/android/server/wm/WindowManagerShellCommand.java
@@ -188,7 +188,7 @@ public class WindowManagerShellCommand extends ShellCommand {
         final Point baseSize = new Point();
 
         try {
-            mInterface.getInitialDisplaySize(displayId, initialSize);
+            mInterface.getOrigDisplaySize(Display.DEFAULT_DISPLAY, initialSize);
             mInterface.getBaseDisplaySize(displayId, baseSize);
             pw.println("Physical size: " + initialSize.x + "x" + initialSize.y);
             if (!initialSize.equals(baseSize)) {
-- 
2.34.1

