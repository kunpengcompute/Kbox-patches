diff --git a/kernel/cgroup/cgroup.c b/kernel/cgroup/cgroup.c
old mode 100644
new mode 100755
index 5d1fdf7c3..403aae5c1
--- a/kernel/cgroup/cgroup.c
+++ b/kernel/cgroup/cgroup.c
@@ -5864,8 +5864,11 @@ int proc_cgroup_show(struct seq_file *m, struct pid_namespace *ns,
 				retval = -ENAMETOOLONG;
 			if (retval < 0)
 				goto out_unlock;
-
-			seq_puts(m, buf);
+			
+			if(mem_cgroup_from_task(current)->memory.max == PAGE_COUNTER_MAX)
+				seq_puts(m, buf);
+			else
+				seq_puts(m, "/");
 		} else {
 			seq_puts(m, "/");
 		}
-- 
2.25.1

